name: Deploy to Production

on:
  push:
    branches: ["simon"]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.VERSION }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: 'slemfowel1'
          password: 'dckr_pat_4IULXOCX0lkPIfxDDWXknNHLquI'

      - name: Get Version Tag
        id: version
        run: echo "VERSION=$(git describe --tags --always)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and Push Docker Image for Production
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            slemfowel1/decaprim:latest
            slemfowel1/decaprim:${{ steps.version.outputs.VERSION }}

      - name: Wait for Docker Hub propagation
        run: sleep 10

      - name: Verify Docker Image Push
        run: |
          echo "Verifying image was pushed..."
          if docker pull slemfowel1/decaprim:latest > /dev/null 2>&1; then
            echo "✅ Docker image successfully pushed and verified."
          else
            echo "❌ Failed to pull docker image"
            exit 1
          fi

      - name: Notify Build Success
        if: success()
        run: echo "✅ Docker image built and pushed successfully for version ${{ steps.version.outputs.VERSION }}"

      - name: Notify Build Failure
        if: failure()
        run: echo "❌ Docker image build failed"

  deploy:
    needs: build-and-push
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Pull Docker Image
        run: docker pull slemfowel1/decaprim:latest

      - name: Stop and Remove Old Container
        run: |
          docker kill decaprim-production 2>/dev/null || true
          docker rm -f decaprim-production 2>/dev/null || true
          sleep 2

      - name: Run Docker Container
        run: |
          docker run -d \
            -p 4043:4043 \
            --name decaprim-production \
            --restart unless-stopped \
            --env-file .env \
            slemfowel1/decaprim:latest

      - name: Health Check
        run: |
          echo "Waiting for application to be ready..."
          for i in {1..40}; do
            if curl -f http://localhost:4043 > /dev/null 2>&1; then
              echo "✅ Application is healthy and ready"
              exit 0
            fi
            echo "Attempt $i/40: Application not ready yet, waiting..."
            sleep 2
          done
          echo "❌ Application failed to become healthy within timeout"
          exit 1

      - name: Verify Application Connection
        run: |
          docker exec decaprim-production curl -X GET http://localhost:4043/api/health \
            -H "Content-Type: application/json" || echo "Warning: Health endpoint not available"

      - name: Clean up old images
        run: |
          docker image prune -a -f --filter "until=72h" || true

      - name: Notify Deployment Success
        if: success()
        run: echo "✅ Production deployment completed successfully for version ${{ needs.build-and-push.outputs.version }}"

      - name: Notify Deployment Failure
        if: failure()
        run: echo "❌ Production deployment failed"
